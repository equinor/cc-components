# Workflow for CI. Runs on push and PRs against main branch.

name: CI

on:
  # Triggers the workflow on push or pull request events but only for the "main" branch
  push:
    branches: ['main']
  pull_request:
    branches: ['main']

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
        # https://github.com/nrwl/nx-set-shas#background
      - uses: nrwl/nx-set-shas@v3
      - name: Use Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '16.x'
          cache: 'npm'
      - name: Install dependencies
        run: npm ci --legacy-peer-deps
      - run: npm nx workspace-lint
      # - run: npx nx format:check
      - run: npm nx affected --target=lint --parallel=3
      - run: npm nx affected --target=test --parallel=3 --ci --code-coverage
      - run: npm nx affected --target=build --parallel=3
