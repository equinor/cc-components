{
  "title": "Delta live table",
  "description": "Schema for the Delta live table configuration",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the delta live table job."
    },
    "maxWorkers": {
      "type": "integer",
      "description": "The maximum number of workers to be used. Defaults to (<number of jobs> - 1)",
      "minimum": 1
    },
    "prodSchedule": {
      "oneOf": [
        {
          "type": "string",
          "pattern": "^\\s*([0-9]|\\*|\\?|\\-|\\/|,|L|W|C|\\#)+(|\\s([0-9]|\\*|\\?|\\-|\\/|,|L|W|C|\\#)+){5}\\s*$",
          "description": "Quartz cron schedule expression for job scheduling. Must consist of 5 or 6 fields (seconds optional)."
        },
        {
          "type": "string",
          "enum": ["continuous"],
          "description": "A specific string value to indicate continuous scheduling."
        }
      ]
    },
    "devSchedule": {
      "oneOf": [
        {
          "type": "string",
          "pattern": "^\\s*([0-9]|\\*|\\?|\\-|\\/|,|L|W|C|\\#)+(|\\s([0-9]|\\*|\\?|\\-|\\/|,|L|W|C|\\#)+){5}\\s*$",
          "description": "Quartz cron schedule expression for job scheduling. Must consist of 5 or 6 fields (seconds optional)."
        },
        {
          "type": "string",
          "enum": ["continuous"],
          "description": "A specific string value to indicate continuous scheduling."
        }
      ]
    },
    "jobs": {
      "type": "array",
      "description": "A list of jobs to execute in the delta live table.",
      "items": {
        "type": "object",
        "properties": {
          "outputTableName": {
            "type": "string",
            "description": "The name of the output table."
          },

          "viewName": {
            "type": "string",
            "description": "The name of the view. (Can be found in the hive metastore)"
          },
          "indexes": {
            "type": "array",
            "description": "A list of clustering indexes to apply. (Typically used for lookup properties like Facility and Project)",
            "items": {
              "type": "string"
            }
          }
        },
        "required": ["outputTableName", "viewName", "indexes"]
      }
    }
  },
  "required": ["name", "jobs", "prodSchedule", "devSchedule"]
}

