// Re-export from workspace-core for backwards compatibility
// The canonical implementation is now in @equinor/workspace-core
export { useAppInsights, AppInsightsProvider } from '@equinor/workspace-core';
export type { ApplicationInsights } from '@equinor/workspace-core';
