{
  "name": "commissioningtask",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/commissioningtask/src",
  "targets": {
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/commissioningtask",
        "commands": [
          {
            "command": "fusion-framework-cli app dev"
          }
        ]
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/commissioningtask",
        "commands": [
          {
            "command": "tsc --noEmit"
          }
        ]
      }
    },
    "build:spa": {
      "executor": "nx:runcommands",
      "options": {
        "cwd": "apps/commissioningtask",
        "commands": [
          {
            "command": "fusion-framework-cli app build"
          }
        ]
      }
    },
    "version": {
      "executor": "@cc-components/plugins:version"
    }
  },
  "tags": []
}
