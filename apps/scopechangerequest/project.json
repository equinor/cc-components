{
  "name": "scopechangerequest",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/scopechangerequest/src",
  "targets": {
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/scopechangerequest",
        "commands": [
          {
            "command": "fusion-framework-cli app dev"
          }
        ]
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/scopechangerequest",
        "commands": [
          {
            "command": "tsc --noEmit"
          }
        ]
      }
    },
    "build:spa": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/scopechangerequest",
        "commands": [
          {
            "command": "fusion-framework-cli app build"
          }
        ]
      }
    },
    "version": {
      "executor": "@cc-components/plugins:version"
    }
  },
  "tags": ["ws"]
}
